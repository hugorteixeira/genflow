% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stt_signature.R
\name{gen_stt_signature}
\alias{gen_stt_signature}
\title{Fingerprint an effective STT configuration}
\usage{
gen_stt_signature(
  service = "openai",
  model = NULL,
  language = NULL,
  stt_args = list()
)
}
\arguments{
\item{service}{STT provider identifier accepted by \code{\link[=gen_stt]{gen_stt()}}.}

\item{model}{Optional provider model identifier. \code{NULL} resolves the same
provider or saved-local default used by \code{\link[=gen_stt]{gen_stt()}}.}

\item{language}{Optional language code. Empty values normalize to \code{NULL}.}

\item{stt_args}{A fully named list of additional \code{\link[=gen_stt]{gen_stt()}} arguments.
Names must be unique. Do not repeat \code{audio}, \code{service}, \code{model}, or
\code{language}; those values are owned by the explicit arguments above.}
}
\value{
A lowercase hexadecimal character scalar. The fingerprint contains
no credential values and is suitable for equality comparison and cache
metadata; it is not a reversible configuration report.
}
\description{
Builds a deterministic, secret-free fingerprint for the effective
speech-to-text configuration that \code{\link[=gen_stt]{gen_stt()}} would use. The signature is
intended for downstream cache validation: it changes when semantic
transcription settings, the effective endpoint, a resolved local model or
executable, or their lightweight artifact signatures change.
}
\details{
Credentials and operational controls do not affect the fingerprint.
Excluded controls include timeouts, polling/retry settings, checkpoint and
output directories, persistence, and output projection. Large-audio
controls that can change the transcript itself (chunk size, duration,
bitrate, and overlap) remain part of the signature and are normalized with
the same validation used by \code{\link[=gen_stt]{gen_stt()}}.
}
\examples{
a <- gen_stt_signature(
  service = "local-openai",
  model = "whisper-large-v3",
  stt_args = list(base_url = "http://127.0.0.1:8000")
)
b <- gen_stt_signature(
  service = "local-openai",
  model = "whisper-large-v3",
  stt_args = list(
    base_url = "http://127.0.0.1:8000",
    timeout_api = 900
  )
)
identical(a, b)

}
