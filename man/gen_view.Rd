% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/display_functions.R
\name{gen_view}
\alias{gen_view}
\title{Visualize a list of generation results}
\usage{
gen_view(
  ...,
  results_generated = NULL,
  grouped = TRUE,
  stats = c("console", "viewer", "hide"),
  debug = FALSE
)
}
\arguments{
\item{...}{Optional individual generation results, plain vectors, or lists to
display. Mixed inputs are flattened automatically.}

\item{results_generated}{Optional legacy argument accepting a list of results
(such as the return value from \code{gen_batch()}). Used when \code{...} is empty.}

\item{grouped}{Logical; when TRUE (default) results and statistics are grouped by result type.}

\item{stats}{Character; choose where statistics are displayed. Use \code{"console"}
(default) to print stats in the console while hiding them in the Viewer,
\code{"viewer"} to render them inside the Viewer only, or \code{"hide"} to suppress
stats entirely.}

\item{debug}{Logical; when TRUE, prints extra diagnostics for rendering media.}
}
\value{
Invisibly returns NULL. Opens the Viewer or prints to console.
}
\description{
Displays generated images, videos, audios, and text in the RStudio Viewer.
Falls back to a readable console output when Viewer is unavailable.
}
\examples{
# gen_view(list(list(status_api = "SUCCESS", response_value = tempfile(fileext=".png"))))

}
