% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tts_functions.R
\name{gen_tts}
\alias{gen_tts}
\alias{gen_tts.default}
\alias{gen_tts.genflow_agent}
\title{Generate speech audio from text}
\usage{
gen_tts(text, ...)

\method{gen_tts}{default}(
  text,
  add = NULL,
  directory = NULL,
  label = NULL,
  service = "openai",
  model = "gpt-4o-mini-tts",
  voice = NULL,
  format = "mp3",
  speed = 1,
  instructions = NULL,
  preview = FALSE,
  timeout_api = 240,
  ...
)

\method{gen_tts}{genflow_agent}(text, ...)
}
\arguments{
\item{text}{Character. The text to synthesize.}

\item{...}{Reserved for future provider-specific arguments.}

\item{add}{Optional character appended to \code{text}.}

\item{directory}{Optional output directory. Defaults to \code{getwd()/audios} if NULL.}

\item{label}{Optional short label used for filenames. If NULL, derived from text.}

\item{service}{Provider identifier (e.g., "openai", "replicate").}

\item{model}{Provider model identifier. Defaults to \verb{gpt-4o-mini-tts}.}

\item{voice}{Optional voice identifier supported by the provider. If NULL,
the first available voice is selected automatically.}

\item{format}{Output audio format. Defaults to "mp3".}

\item{speed}{Numeric speech speed (0.25 to 4.0). Defaults to 1.}

\item{instructions}{Optional instructions to style the voice (OpenAI only).}

\item{preview}{Logical; attempt to open audio after save if interactive.}

\item{timeout_api}{Numeric; request timeout in seconds.}
}
\value{
Invisibly returns a list with \code{response_value} (saved file path),
\code{status_api}, \code{status_msg}, \code{service}, \code{model}, \code{duration}, \code{saved_file},
and metadata such as \code{voice}, \code{format}.
}
\description{
High-level text-to-speech (TTS) wrapper that dispatches to provider-specific
implementations (currently OpenAI and Replicate). Returns the saved audio file path.
}
\examples{
# Minimal example (requires OpenAI API key)
# tts <- gen_tts("Hello world", service = "openai")
# tts$response_value

}
