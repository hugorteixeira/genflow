% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stt_functions.R
\name{gen_stt}
\alias{gen_stt}
\alias{gen_stt.default}
\alias{gen_stt.genflow_agent}
\title{Transcribe speech from an audio file}
\usage{
gen_stt(audio, ...)

\method{gen_stt}{default}(
  audio,
  service = "openai",
  model = NULL,
  language = NULL,
  prompt = NULL,
  directory = NULL,
  label = NULL,
  save_txt = TRUE,
  convert = TRUE,
  diarize = TRUE,
  diarize_speakers = FALSE,
  timestamps = FALSE,
  timeout_api = 240,
  timeout_per_audio_minute = 60,
  poll_interval = 5,
  max_poll_seconds = 600,
  executable = NULL,
  native_engine = NULL,
  native_backend = NULL,
  native_quant = NULL,
  native_device = NULL,
  max_new_tokens = NULL,
  base_url = NULL,
  api_key = NULL,
  response_format = "json",
  ...
)

\method{gen_stt}{genflow_agent}(audio, ...)
}
\arguments{
\item{audio}{Character path or URL to an audio file (e.g., .mp3, .ogg, .wav).}

\item{...}{Reserved for future provider-specific arguments.}

\item{service}{Provider identifier (e.g., "openai", "groq", "assemblyai",
"cloudflare", "voicegain", "hf", "replicate", \code{"local-native"}, or
\code{"local-openai"}).}

\item{model}{Provider model identifier. If NULL, a sensible default is used
per provider. For \code{service = "local-native"}, this is normally a local
model file. The CrispASR engine also accepts \code{"auto"} or an explicit
\verb{hf://OWNER/REPO:FILE} reference. For copy-and-paste convenience,
\verb{hf://OWNER/REPO/FILE} and
\verb{https://huggingface.co/OWNER/REPO/blob/main/FILE} are accepted and
normalized to the same form. A downloaded model selected in the Models
catalog is passed as its cache filename and resolved only inside the
managed CrispASR cache. An explicit \code{"auto"} remains a CrispASR registry
request and is not replaced by a legacy saved model.}

\item{language}{Optional language code (e.g., "en", "pt"). If NULL, provider
auto-detection is used when supported.}

\item{prompt}{Optional prompt to guide transcription (provider-specific).}

\item{directory}{Optional output directory for saved transcripts when
\code{save_txt = TRUE}. Defaults to \verb{~/.genflow/transcripts} when NULL.}

\item{label}{Optional short label used for saved filenames.}

\item{save_txt}{Logical; save transcript to disk if TRUE.}

\item{convert}{Logical; if TRUE, attempt ffmpeg conversion for unsupported
audio formats.}

\item{diarize}{Logical; when \code{TRUE}, request speaker attribution from native
adapters that expose a model-native opt-in mode (currently CrispASR
Granite Speech 4.1 Plus), then expose and save speaker-attributed text
whenever the selected model/provider returns speaker metadata. This
output control does not add diarization to a model that lacks it; use
\code{diarize_speakers} for CrispASR's generic speaker pipeline.}

\item{diarize_speakers}{Logical; explicit opt-in to CrispASR's
session-scoped speaker diarization for \code{service = "local-native"}.
\code{TRUE} passes \code{--diarize-speakers}, which uses native GGUF Pyannote
segmentation plus TitaNet clustering to add anonymous speaker labels
that remain stable within one input recording. It requires
\code{diarize = TRUE} and the CrispASR engine. The default \code{FALSE} avoids
model downloads and CPU overhead for callers that do not request it.}

\item{timestamps}{Logical; when \code{TRUE}, include the time range of every
diarized segment. Defaults to \code{FALSE}, which merges consecutive segments
from the same speaker into readable turns while retaining labels such as
\verb{[S01]} and \verb{[S02]}.}

\item{timeout_api}{Numeric; base request timeout in seconds.}

\item{timeout_per_audio_minute}{Non-negative numeric; additional timeout
seconds for every minute (or partial minute) of the input file. The
default \code{60} adds one processing minute per minute of audio. Set to \code{0}
for a fixed \code{timeout_api}.}

\item{poll_interval}{Numeric; polling interval (seconds) for async providers.}

\item{max_poll_seconds}{Numeric; max polling time for async providers.}

\item{executable}{Optional executable for \code{service = "local-native"}.
Resolution uses \code{GENFLOW_STT_NATIVE_EXECUTABLE}, the saved local inference
configuration, and finally the selected engine's executable on \code{PATH}.}

\item{native_engine}{Native runtime for \code{service = "local-native"}:
\code{"auto"}, \code{"crispasr"}, or \code{"moss-transcribe"}. Engines support model
architectures explicitly; no native runtime can execute every model in
the Hugging Face catalog.}

\item{native_backend}{Optional model architecture/backend understood by the
selected engine, such as \code{"whisper"}, \code{"parakeet"}, \code{"canary"},
\code{"granite-4.1"}, or \code{"moss-diarize"} for CrispASR. Leave NULL to use
model auto-detection for an explicit local or \verb{hf://} model.
CrispASR \code{model = "auto"} requires a backend.}

\item{native_quant}{Optional CrispASR registry quantization preference, such
as \code{"q8_0"}. It is passed as \code{--model-quant} only when
\code{service = "local-native"} uses CrispASR with \code{model = "auto"}. The value
selects a requested filename; neither genflow nor CrispASR guarantees that
the corresponding remote artifact exists. Defaults to
\code{GENFLOW_STT_NATIVE_QUANT}, then the saved \code{stt_native_quant} setting.}

\item{native_device}{Native accelerator for \code{service = "local-native"}:
\code{"auto"}, \code{"cpu"}, \code{"vulkan"}, \code{"hip"}, \code{"cuda"}, or \code{"metal"}.
Support is engine-specific; CrispASR does not expose HIP and should use a
Vulkan-enabled build on AMD hardware.}

\item{max_new_tokens}{Optional generation limit. For CrispASR MOSS Diarize,
\code{NULL} derives a duration-aware budget between 2,048 and 65,536 tokens so
long single-window transcriptions are not silently cut at the runtime's
1,024-token default. An explicit positive integer always wins. This is an
output budget; it does not extend the model's documented audio context.}

\item{base_url}{Optional base URL for \code{service = "local-openai"}. Defaults
to \code{GENFLOW_STT_BASE_URL}, then \verb{http://127.0.0.1:8000}.}

\item{api_key}{Optional bearer token for \code{service = "local-openai"}.
Defaults to \code{GENFLOW_STT_API_KEY}; no authorization header is sent when
both are empty.}

\item{response_format}{Response format requested from a local
OpenAI-compatible server, typically \code{"json"} or \code{"verbose_json"}.}
}
\value{
Invisibly returns a plain list with \code{response_value} (plain
transcribed text), \code{status_api}, \code{status_msg}, \code{service}, \code{model},
\code{duration}, \code{saved_file}, and metadata such as \code{audio}. When
\code{diarize = TRUE}, diarized results additionally include
\code{diarized_transcript} and \code{saved_metadata_file} when saving is enabled.
Timestamps are optional and disabled by default. As with the other
generators, the call writes a concise status summary to the console while
the returned object keeps the regular list representation.
}
\description{
High-level speech-to-text (STT) wrapper that dispatches to provider-specific
implementations (OpenAI, Groq, AssemblyAI, Cloudflare, Voicegain, Hugging
Face, registered native engines, and local OpenAI-compatible servers).
Returns the transcribed text and optionally saves a \code{.txt} file. When the
provider returns speaker-attributed segments and \code{diarize = TRUE}, the text
file preserves readable speaker turns and a JSON sidecar preserves the
structured metadata.
}
\details{
For a \code{genflow_agent}, supply \code{audio_override} through \code{...} to
transcribe a different input file without changing the saved agent.
}
\examples{
# Minimal example (requires a provider API key)
# res <- gen_stt("audio.ogg", service = "openai")
# res$response_value

}
