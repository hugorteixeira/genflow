% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom_provider_functions.R
\name{set_provider_openai_compat}
\alias{set_provider_openai_compat}
\title{Register a custom OpenAI-compatible provider}
\usage{
set_provider_openai_compat(
  id,
  label = NULL,
  base_url = NULL,
  base_urls = NULL,
  api_key_env = NULL,
  model_env = NULL,
  default_model = "local-model",
  chat_paths = c("/v1/chat/completions", "/chat/completions"),
  model_paths = c("/v1/models", "/models"),
  auth_header = "Authorization",
  auth_prefix = "Bearer",
  extra_headers = NULL,
  api_key_required = NULL,
  supports_tools = TRUE,
  supports_vision = TRUE,
  supports_reasoning = FALSE,
  supports_plugins = FALSE,
  reasoning_field = NULL,
  plugins_field = NULL,
  max_tokens = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{id}{Provider id (lowercase key used in \code{service}), e.g. \code{"mycloud"}.}

\item{label}{Display label used in interfaces and listings.}

\item{base_url}{Optional single base URL.}

\item{base_urls}{Optional character vector of base URLs (failover order).}

\item{api_key_env}{Environment variable name holding the API key.}

\item{model_env}{Optional environment variable name with a default model.}

\item{default_model}{Fallback model when none is provided/discovered.}

\item{chat_paths}{Candidate chat endpoints (relative paths or absolute URLs).}

\item{model_paths}{Candidate model-list endpoints.}

\item{auth_header}{Header name for auth (use \code{""} to disable auth header).}

\item{auth_prefix}{Auth scheme prefix, e.g. \code{"Bearer"} or \code{""}.}

\item{extra_headers}{Optional named headers added to every request.}

\item{api_key_required}{Logical; whether missing API key should error.}

\item{supports_tools}{Logical capability flag.}

\item{supports_vision}{Logical capability flag.}

\item{supports_reasoning}{Logical capability flag.}

\item{supports_plugins}{Logical capability flag.}

\item{reasoning_field}{Optional payload field for reasoning, e.g. \code{"reasoning"}.}

\item{plugins_field}{Optional payload field for plugins/extensions.}

\item{max_tokens}{Optional max tokens sent as \code{max_tokens}.}

\item{overwrite}{Logical; overwrite existing custom provider with same id.}
}
\value{
Invisibly returns the normalized provider configuration.
}
\description{
Saves a provider configuration that can be used as \code{service} in \code{gen_txt()}
and in \code{gen_update_models(provider = "<id>")}.
}
\examples{
\dontrun{
set_provider_openai_compat(
  id = "mycloud",
  label = "My Cloud",
  base_urls = "https://llm.example.com",
  api_key_env = "MYCLOUD_API_KEY",
  default_model = "my-model"
)
}
}
